## Packages
recharts | For dashboard analytics charts (attendance trends, marks distribution)
date-fns | For date formatting and calendar logic
framer-motion | For smooth page transitions and micro-interactions
lucide-react | For beautiful iconography (already in base, but ensuring availability)
clsx | For conditional class merging (already in base)
tailwind-merge | For merging tailwind classes (already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
